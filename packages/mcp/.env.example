# Where the Stela app/API is reachable
STELA_API_URL=http://localhost:5173
# Per-user publish key (matches the app's STELA_API_KEY for v1)
STELA_API_KEY=dev-publish-key
# Optional: per-request timeout (ms) for MCP -> Stela API calls. Default 30000.
# STELA_TIMEOUT_MS=30000
